# claude-code-router-config
My Claude Code Router config. (Along with some nifty extras.)

## NOTE

`mcp-claude-code.sh` MUST be run per-project, but the claude code router config only has to be implemented once.
